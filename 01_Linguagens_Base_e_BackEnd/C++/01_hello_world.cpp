#include <iostream> // Biblioteca para fluxos de entrada e saída no console
#include <string>   // Biblioteca para manipulação de strings modernas

// Função inicial padrão e obrigatória para execução do programa

int main() {

// Usamos o fluxo de saída std::cout para enviar ao print ou a variável para a tela
// std::endl é usado para adicionar uma nova linha e garantir que o buffer seja limpo"
// Retorna 0 para indicar que o programa terminou com sucesso

    std::cout << "Hello World" << std::endl; 
    return 0; 


}